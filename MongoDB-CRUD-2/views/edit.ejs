<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Signup UI</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- BOOTSTRAP CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        /* FULL PAGE CENTER ALIGN */
        body {
            background: #f4f6f9;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        /* MAIN CARD */
        .signup-card {
            width: 500px;
            display: flex;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            background: #fff;
        }

        /* RIGHT PANEL */
        .right-panel {
            width: 100%;
            padding: 40px;
        }

        /* PROFILE PREVIEW */
        .preview {
            width: 90px;
            height: 90px;
            border-radius: 50%;
            object-fit: cover;
            display: block;
            margin: 10px auto;
            border: 2px solid #ddd;
        }

        /* BUTTON STYLE */
        .btn-primary {
            width: 100%;
            border-radius: 8px;
        }

        /* SMALL SCREEN RESPONSIVE */
        @media (max-width: 768px) {
            .signup-card {
                flex-direction: column;
                width: 95%;
            }

            .left-panel,
            .right-panel {
                width: 100%;
            }
        }
    </style>
</head>

<body>

    <div class="signup-card">

        <!-- RIGHT SIDE FORM -->
        <div class="right-panel">

            <h2 class="mb-4 text-center">Update Profile</h2>

            <form action="/updateuser/<%-data._id%>" method="post" enctype="multipart/form-data">

                <!-- PROFILE IMAGE -->
                <div class="text-center mb-3">
                    <input type="file" id="userProfile" name="userProfile" class="form-control" accept="image/*">
                    <img src="/uploads/<%-data.userProfile%>" class="mt-3" alt="" height="100px" width="100px"
                        style="border-radius: 50%;">
                    <img id="previewImage" class="preview mt-3" src="https://via.placeholder.com/90"
                        alt="PROFILE PREVIEW">
                </div>

                <!-- NAME -->
                <div class="row mb-3">
                    <div class="col">
                        <input type="text" class="form-control" id="userName" name="userName"
                            placeholder="Enter your name" value="<%-data.userName%>">
                    </div>
                </div>

                <!-- EMAIL -->
                <div class="mb-3">
                    <input type="email" class="form-control" id="userEmail" name="userEmail"
                        placeholder="Enter your email" value="<%-data.userEmail%>">
                </div>

                <!-- PHONE -->
                <div class="mb-3">
                    <input type="text" class="form-control" id="userPhone" name="userPhone"
                        placeholder="Enter your contact number" value="<%-data.userPhone%>">
                </div>

                <!-- ADDRESS -->
                <div class="mb-3">
                    <textarea class="form-control" id="userAddress" name="userAddress"
                        placeholder="Enter your address"><%-data.userAddress%></textarea>
                </div>

                <!-- SUBMIT -->
                <button type="submit" class="btn btn-primary">Update</button>

            </form>

        </div>
    </div>

    <script>
        // PROFILE IMAGE PREVIEW
        document.getElementById("userProfile").addEventListener("change", function (event) {
            const file = event.target.files[0];

            if (file) {
                const reader = new FileReader();

                reader.onload = function (e) {
                    document.getElementById("previewImage").src = e.target.result;
                };

                reader.readAsDataURL(file);
            }
        });
    </script>

</body>

</html>